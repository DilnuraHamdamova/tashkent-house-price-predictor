# Capstone evidence matrix

This matrix uses the official eight criteria. A location includes a file plus the exact section, notebook cell group, function, or runnable step so a reviewer can navigate without guessing.

| Criterion | What I claim | Exact evidence location | Why this is proof | Status | Gap / risk | Next action |
|---|---|---|---|---|---|---|
| 1. Problem Definition and Project Alignment | The project predicts historical Tashkent apartment listing prices for buyers, sellers, agents, and analysts as an Individual Project. | `README.md` → “Selected track” + “Problem and scope”; `submission/PROJECT_BRIEF.md` → “Project information”, “Problem”, “Success criteria” | Both documents state the same user, regression task, raw input, USD output, measurable thresholds, scope, and non-goals. | **YELLOW** | Repository cannot prove that a mentor approved the brief before implementation. | Student confirms/retains the mentor approval message and uses exactly “Individual Project Track” in the defense. |
| 2. Data and Preprocessing Pipeline | Dataset source, license, quality issues, feature decisions, split, and leakage-safe transformations are documented and implemented. | `data/README.md` → “Source and license”, “Schema”, “Quality”; `notebooks/01_data_audit.ipynb` → cells 2–10; `src/data.py` → `load_dataset`, `prepare_features`; `src/model.py` → `_preprocessor`, `train_best_model` | These show 7,421 source rows, 696 duplicates removed before splitting, input validation, engineered floor ratio, in-pipeline encoding/scaling, and district-stratified train/test/CV splits. | **GREEN** | Outliers are not source-verified. | Explain why they were retained and how MAE/RMSE expose their effect. |
| 3. Modeling and Experiments | A naive baseline and three meaningful trained approaches were compared before selecting Random Forest by training-only CV MAE. | `reports/model_comparison.csv`; `notebooks/02_experiments.ipynb` → cells 2–6; `src/model.py` → `candidate_models`, `train_best_model`; `artifacts/metrics.json` → `experiments` | The structured log contains Median, Log Ridge, Random Forest, and Gradient Boosting CV metrics, parameters, fit time, selection rule, and random seed. | **GREEN** | Hyperparameters are controlled rather than exhaustively tuned. | Defend simplicity/reproducibility and propose nested tuning as future work. |
| 4. Evaluation and Error Analysis | The selected model is evaluated on 1,345 unseen test listings and compared with baseline; difficult listings and district slices are analyzed. | `reports/protected_test_metrics.csv`; `reports/largest_errors.csv`; `reports/district_metrics.csv`; `reports/results.md` → “Protected test evaluation”, “Largest errors”, “District slices” | The evidence reports MAE, RMSE, R², MAPE, baseline comparison, ten concrete residuals, and per-district sample counts/metrics. | **GREEN** | Rare luxury listings and sparse districts have unstable errors. | Show the Mirobod $800k error and the Bektemir/Yangihayot sample-count warning during defense. |
| 5. End-to-End Implementation and Delivery | A raw apartment record passes through validation, feature engineering, saved preprocessing, and the trained model to a prediction and warnings. | `demo.ipynb` → setup cell 2, artifact cell 4, inference cell 6, invalid-input cell 8; `src/predict.py`; `src/model.py` → `load_artifact`, `predict_price`; `artifacts/house_price_pipeline.joblib` | The same serialized sklearn pipeline used after training is loaded for inference; a normal example returns about $53,532 and an impossible floor raises a clear error. | **GREEN** | Current-price interpretation would be unsafe. | Always describe output as an estimated **2019 asking price in USD**. |
| 6. Documentation and Reproducibility | A clean environment can install dependencies and reproduce data loading, inference, tests, lint, and the demo route. | `README.md` → “Reproduce locally”, “Colab demo”; `requirements.txt`; `reports/clean_run_check.md`; `.github/workflows/ci.yml`; GitHub Actions run `32002361933` | The clean-run record gives commands/results, while public CI independently installs the package and passes lint plus six tests. | **GREEN** | A named peer/mentor reproduction is not recorded yet. | Ask one person to run Colab top-to-bottom and append their date/result before defense. |
| 7. Responsible AI and Limitations | Dataset age, asking-price labels, representation bias, privacy, unsafe uses, and human review are project-specific. | `README.md` → “Limitations and responsible use”; `data/README.md` → “Limitations”; `submission/PROJECT_BRIEF.md` → “Responsible AI and limitations” | These sections name sparse districts, 2019 drift, location bias, missing features, absence of direct PII, no retention of demo inputs, required human review, and prohibited financial/legal uses. | **GREEN** | Geographic bias is described but not causally measured. | State that district metrics diagnose performance variation, not fairness or causality. |
| 8. Presentation, Demo, and Q&A | A fixed five-minute story, live demo route, evidence-backed question bank, and defense slides are prepared. | `presentation/Tashkent_House_Price_Defense.pptx`; `docs/defense_pitch_outline.md`; `docs/defense_question_bank.md`; `docs/action_plan.md` | The deck and pitch follow the required timing, include one real input-to-output path, and connect likely answers to exact evidence. | **YELLOW** | Timed rehearsal, live defense, and Q&A performance can only be completed by the student. | Run two timed rehearsals, one peer “show me where” audit, and record results in `PROJECT_STATUS.md`. |

## Readiness gate

**YELLOW — two external confirmations remain:** mentor approval evidence for the Individual Project Brief and the student's timed/peer rehearsal. The technical repository and demo route are Green; verbal claims must not replace these remaining actions.
